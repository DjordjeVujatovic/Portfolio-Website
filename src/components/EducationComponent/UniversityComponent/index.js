import React from 'react';

const UniversityComponent = ({ education }) => { //eslint-disable-line
  return (
    <div className="universityComponent">University Component</div>
  );
};

export default UniversityComponent;
