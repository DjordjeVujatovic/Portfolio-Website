// Action

export const SHOW_READING = 'SHOW_READING';

// Action Creator

export const showReading = () => ({
  type: SHOW_READING,
  payload: null,
});
// Action

export const SHOW_FAVORITES = 'SHOW_FAVORITES';

// Action Creator

export const showFavorites = () => ({
  type: SHOW_FAVORITES,
  payload: null,
});

export const SHOW_FUTURE_READS = 'SHOW_FUTURE_READS';

// Action Creator

export const showFutureReads = () => ({
  type: SHOW_FUTURE_READS,
  payload: null,
});

// Action

export const CLOSE_LIST = 'CLOSE_LIST';

// Action Creator

export const closeList = () => ({
  type: CLOSE_LIST,
  payload: null,
});

